{
  "name": "@password-manager/extension",
  "version": "1.0.0",
  "description": "Zero-knowledge password manager browser extension (Manifest V3)",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@password-manager/crypto-engine": "file:../crypto-engine"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "@types/chrome": "^0.0.260",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
